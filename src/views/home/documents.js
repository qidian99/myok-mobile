import React from 'react';
import {View, Text} from 'react-native';
import {globalStyles} from 'styles/index';

const Documents = () => (
  <View style={globalStyles.center}>
    <Text style={globalStyles.title}>Documents</Text>
  </View>
);

export default Documents;
